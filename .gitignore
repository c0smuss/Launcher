# Python
__pycache__/
*.pyc

# User data (machine-specific, may contain personal paths/profiles)
launch_config.json
launch_config.json.*
launcher_settings.json
app_statistics.json
crash_history.json
app.log
app.log.*

# Exported profiles (may contain personal paths)
*.launchprofile.json

# Old snapshot kept locally only
Launch-v1/
